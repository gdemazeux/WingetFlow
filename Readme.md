WingetFlow
==================

A winget plugin for the [Flow launcher](https://github.com/Flow-Launcher/Flow.Launcher).
With this plugin, you can search, install, or upgrade applications using the Windows Package Manager (winget) directly from Flow Launcher.

### Usage

    wget <your search>

The search can be performed using the package name or ID.